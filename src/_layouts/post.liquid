<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://unpkg.com/prismjs@1.20.0/themes/prism.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ '/assets/css/tailwind.css' | url }}">
    <title>
      wayson blog |
      {{ title }}
    </title>
  </head>
  <body class="bg-stone-100 flex justify-center text-black w-screen">
    <div class="flex flex-col w-8/12">
      {% include 'nav.liquid' %}
      <div class="mb-3">
        <h1 class="text-2xl font-bold">{{ title }}</h1>
        <h2 class="text-gray-500">{{ page.date.toDateString }}</h2>
      </div>
      <div class="markdown min-w-3xl max-w-3xl">
        {{ content }}
      </div>
      {% include 'footer.liquid' %}
    </div>
  </body>
</html>
