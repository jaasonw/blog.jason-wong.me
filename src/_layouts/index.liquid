<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://unpkg.com/prismjs@1.20.0/themes/prism.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ '/assets/css/tailwind.css' | url }}">
    <title>
      wayson blog |
      {{ title }}
    </title>
  </head>
  <body class="bg-stone-100 flex justify-center text-black w-screen">
    <div class="flex flex-col w-8/12">
      {% include 'nav.liquid' %}
      <div class="markdown">
        {{ content }}
      </div>

      <h1 class="text-xl font-bold mt-5">Articles</h1>
      <ul>
        {%- for post in collections.posts reversed -%}
          <li class="flex flex-col">
            <a class="text-sky-600 font-semibold hover:underline" href="{{ post.url }}">{{ post.data.title }}</a>
            <span class="text-gray-500 text-sm">{{ post.date.toDateString }}</span>
          </li>
        {%- endfor -%}
      </ul>
      {% include 'footer.liquid' %}
    </div>
  </body>
</html>
